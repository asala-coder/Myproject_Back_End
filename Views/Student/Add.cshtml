@using MyProject.Models
@model MyProject.Models.Student

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "_Layout";
}
<h1>Add New Student To The Portal</h1>
<div class="form-container">
    <form method="post" asp-action="Add" asp-controller="Student">
        <div class="mt-3">
            <label class="form-label">Name</label>
            <input type="text" class="form-control" asp-for="Name" />
            <span asp-validation-for="Name"></span>

        </div>

        <div class="mt-3">
            <label class="form-label">age</label>
            <input type="text" class="form-control" asp-for="Age" />
            <span asp-validation-for="Age"></span>

        </div>
        <div class="mt-3">
            <label class="form-label">Fees</label>
            <input type="text" class="form-control" asp-for="Fees" />
            <span asp-validation-for="Fees"></span>

        </div>
        <div>
            <label asp-for="department"></label>
            <select asp-for="department" class="field" asp-items="@Html.GetEnumSelectList(typeof(Department))">
                <option value=""></option> select
            </select>
            <span asp-validation-for="department"></span>
        </div>
        <div>
            <label  asp-for="semester"></label>
            <select asp-for="semester" class="field" asp-items="@Html.GetEnumSelectList(typeof(Semseter))">
                <option value=""></option> select
            </select>
            <span asp-validation-for="department"></span>
        </div>
        <div class="mt-3">
            <button type="submit" class="btn btn-primary">Save</button>
        </div>

    </form>
</div>